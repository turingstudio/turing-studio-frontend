import styled from 'styled-components'
import { Colors } from '../../../constants/colors'

const ScreenBuilderStyled = styled.div`
  height: 1000px;
  width: 100%;
  margin: 10px;
  padding: 10px;
  border: 1px solid ${Colors.dividerColor};
`

export default ScreenBuilderStyled
